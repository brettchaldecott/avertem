var version;var hash = getSeed();
var originalHash = hash;

${0003_keto_transaction_module_24_sourceCallList}
${0003_keto_transaction_module_24_headerCallList}

var test_signature = "${TRANSACTION_MODULE_OVERLAP_SIGNATURE_2}";
var checkHash = executeCallBacks(hash);
var checkHash2 = executeCallBacks(hash);

if (vectorEqual(checkHash,checkHash2)) {
    hash = originalHash;
    for (var i = 0; i < 9; ++i) {
        hash = generateHash(hash);
    }
} else {
    hash = checkHash;
}
return hash
