/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

#include <boost/dll/alias.hpp>
#include <boost/dll/shared_library.hpp>
#include <boost/shared_ptr.hpp>

#include "keto/common/Log.hpp"

#include "keto/obfuscate/MetaString.hpp"
#include "keto/crypto/Containers.hpp"
#include "keto/crypto/SecureVectorUtils.hpp"

#include "keto/block/BlockModuleManager.hpp"
#include "keto/common/MetaInfo.hpp"

${0020_keto_block_module_headerList}

namespace keto {
namespace block {

keto::crypto::SecureVector ${BLOCK_MODULE_SIGNATURE_METHOD}() {
    std::string signature = OBFUSCATED("${BLOCK_MODULE_SIGNATURE}");
    return keto::crypto::SecureVectorUtils().copyStringToSecure(signature);
}

keto::crypto::SecureVector ${BLOCK_MODULE_KEY_METHOD}() {
    std::string key = OBFUSCATED("${BLOCK_MODULE_KEY}");
    return keto::crypto::SecureVectorUtils().copyStringToSecure(key);
}

keto::software_consensus::SourceVersionMap ${BLOCK_MODULE_SOURCE_VERSION_METHOD_MAP}() {
    keto::software_consensus::SourceVersionMap sourceVersionMap;

    ${0020_keto_block_module_headerClassList}
    ${0020_keto_block_module_sourceClassList}

    return sourceVersionMap;
}

keto::software_consensus::ConsensusHashScriptInfoVector ${BLOCK_MODULE_CONSENSUS_HASH_SCRIPT_METHOD}() {
    keto::software_consensus::ConsensusHashScriptInfoVector consensusHashScript;

    return consensusHashScript;
}

keto::software_consensus::ConsensusHashGeneratorPtr BlockModuleManager::getConsensusHash() {

    keto::software_consensus::SourceVersionMap sourceVersionMap = ${BLOCK_MODULE_SOURCE_VERSION_METHOD_MAP}();

    keto::software_consensus::ConsensusHashScriptInfoVector consensusVector = ${BLOCK_MODULE_CONSENSUS_HASH_SCRIPT_METHOD}();
    
    
    keto::software_consensus::ConsensusHashGeneratorPtr consensusHashGenerator = 
        keto::software_consensus::ConsensusHashGenerator::initInstance(
            consensusVector,
            &${BLOCK_MODULE_SIGNATURE_METHOD},
            &${BLOCK_MODULE_KEY_METHOD},
            sourceVersionMap);
    
    return consensusHashGenerator;
}


}
}

